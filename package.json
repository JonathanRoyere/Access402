{
  "name": "access402",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc -b --pretty",
    "clean": "find packages -name dist -type d -prune -exec rm -rf {} + && find . -name '*.tsbuildinfo' -delete",
    "smoke": "node work/smoke-test.mjs",
    "test:core": "npm run build && node --test test/core/payment-challenge.test.mjs test/core/payment-gate.test.mjs",
    "test:all": "npm run build && node --test test/core/payment-challenge.test.mjs test/core/payment-gate.test.mjs test/storage-memory/in-memory-payment-store.test.mjs test/express/access402-middleware.test.mjs"
  },
  "devDependencies": {
    "@types/node": "^22.10.1",
    "typescript": "^5.8.3"
  },
  "engines": {
    "node": ">=20"
  }
}
